Write a Java program that runs two threads simultaneously, but one thread runs and the
other thread does not start executing before the first thread completes execution. One
thread must be created using Thread class and the other using Runnable interface. One
thread should print names of flowers and the other should print names of fruits.
