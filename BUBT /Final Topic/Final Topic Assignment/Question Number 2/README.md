A system needs to store and display different types of employee information such as
employee ID (Integer), designation (String), and salary (Double) using a reusable
structure. Write a Java program using a generic class that stores these values, uses a
constructor to initialize them and displays the values using a method. Create an object in
the main method and show the output for ID, designation and salary.
